我想把我的HTML里面的图用JavaScript制作，动画效果通过 requestAnimationFrame 实现，
也可以视情况使用Three.js的JavaScript 3D 库。
你要给我进行修改原始代码的新代码对，放在一个代码块里面一并给我，要不然我复制的格式不对。
你要严格对照原代码的相关行代码给出修改代码对，代码对要连贯的。
要保留了原始格式而不是使用转义符号，换行就是换行，不能使用\n这种，每一个原始空格也要要。
原始文本没有空格的就不要擅自加。
你给我的代码对格式一定是要像这样的：
Path:index.html
search:《………………………………》
replace:《………………………………》
search部分《》里的内容是要被替换的原文完整代码而且一个字符都不能省略！！！要原原本本的。
replace部分《》里的内容是要替换原始代码的新代码。

用替换模式修改，要严格的匹配原代码每一行的所有字符包括空格、分行、缩进等等，要不然找不到原文要替换的地方的。

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
深刻阅读一下这篇文章或要求，从物理逻辑的视角，做个中文解读（1000字）。
要从网上尽量收搜丰富的权威信息。要关注里面的重要细节内容。
核心观点和数据要有10个交互式动画展示（分别是概念交互、流程交互、数据交互、）,可以用你想到的任何方法制作，越丰富越好。
里面涉及的公式要用LATEX格式，要调用unpkg.com库源正确渲染，文本行里的公示也要渲染。
整个代码用html实现。
附件为具体的文章:
要求：

……………………………………………………………………………………………………………………………………………………………………………………………………………………………………

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
从物理逻辑视角分析这篇文章，撰1000字中文解读，用1000行完整、完美的HTML代码呈现，不能出现逻辑问题。
主要以动画方式呈现，至少5个动画，使用 JavaScript 的 Canvas API 或专门的 JavaScript 图表/动画库（如 Chart.js, D3.js, P5.js 等）来实现动画。（如概念动画、流程动画、数据动画、对比动画、时间轴动画、探索与模拟动画、因果链动画、空间结构动画、假设验证动画、分层解析动画、网络动画等类型）
这些动画要有交互按钮或者播放键，展示核心观点、数据。公式用普通的文本模式就可以了（类似F = G * (m1 * m2) / r^2），尽量用通用符号，不要用LATEX公式。内容需丰富权威，细节准确。

最好调用GEMINI2.5PRO 的API渠道来进行编码。

（编码后有问题让Claude修改）
Pyodide: Pyodide 是一个强大的工具，它允许在浏览器中运行 Python。


用那个www.alphaxiv.org的GEMINIPRO好用应该是调用了API（用那个www.alphaxiv.org的GEMINIPRO好用应该是调用了API）
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
调用你的工具箱，在网上收集丰富权威的资料、信息、论文等信息，完成下面的回答：

you need to modify <file>, use write_to_file. Avoid replace_in_file.严格按照下面的要求修改INDEX.HTML文件。

只用 replace_in_file，把 src/index.html的

git submodule update --init --recursive 小仓库更新命令 要到那个文件夹下

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
你帮我写个方案，就是主要核心功能就是用LLM对网页代码进行分析，
提取出独立的模块，相关之间没有逻辑影响或者影响很小。
后续对单独的模块进行代码修改而不影响其它的内容，
最后进行整合成一个独立的HTML。你写一个PYTHON的实现方案。